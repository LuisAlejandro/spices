# Spices does not require application-specific environment variables.
# Local development uses Docker via the Makefile (make console, make lint, make format, make test).

# Optional: used by scripts/docker-tests/local-docker-test.sh when running local Docker tests.
# LOG_LEVEL=INFO
# JSON_LOG=false
# ENABLE_COLORS=true
